
/**
 * toast.tsx
 * 
 * Componente de toast (notificação) para exibir mensagens temporárias na interface.
 * Baseado no Radix UI Toast Primitive, este componente permite exibir notificações
 * que desaparecem automaticamente após um tempo determinado.
 * 
 * Este arquivo define a estrutura visual e comportamental dos toasts, incluindo:
 * - Posicionamento na tela
 * - Animações de entrada e saída
 * - Estilos visuais
 * - Comportamento e interações
 */

// Re-export from the hooks directory
import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
